import { environment } from 'src/environments/environment';

export const AppConstants = Object.freeze({
    API_URL: `${environment.API_URL}/api`,
    API_URL_IMAGES: `${environment.API_URL}/assets/img/flags`
});
